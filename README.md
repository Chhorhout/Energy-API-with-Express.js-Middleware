# Express Middleware Assignment

Energy API secured with Express.js middleware (IP filter, CORS, rate limit, Bearer token, Basic Auth).

## Run

```bash
npm install
npm start
```

Server runs at http://localhost:3001

## Credentials

**Bearer Token** (for `/api/oil-prices`):
```
energy-api-secret-token-2024
```

**Basic Auth** (for `/dashboard`):
- Username: `admin`
- Password: `energy123`
